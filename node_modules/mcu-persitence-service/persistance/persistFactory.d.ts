import { PersistDB, DBOptions } from "../common/common";
export declare class PersistFactory {
    private logger;
    private options;
    constructor(logger: any, options: DBOptions);
    getDB: () => PersistDB;
}
