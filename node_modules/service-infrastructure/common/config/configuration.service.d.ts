import { OnApplicationShutdown, OnModuleInit } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { DynamicConfig } from './dynamic.config.service';
import { MculoggerService } from '../logs/mculogger.service';
export declare class ConfigurationService implements OnModuleInit, OnApplicationShutdown {
    private readonly configService;
    private readonly dynamicConfig;
    private readonly logger;
    private flattenConfig;
    private readonly confKey;
    private timeout;
    private confPath;
    private PLATFORM_TYPE;
    private serviceConf;
    constructor(configService: ConfigService, dynamicConfig: DynamicConfig, logger: MculoggerService);
    onModuleInit(): Promise<void>;
    onApplicationShutdown(signal?: string): any;
    get(propertyPath: string, defaultValue?: any): any;
    updateConfiguration(): Promise<void>;
    getConfig(): Object;
}
