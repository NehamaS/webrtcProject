import { DBType } from "mcu-persitence-service/common/common";
export interface ETCDSettings {
    host: string;
    port: number;
    ttl: number;
    refreshInterval: number;
}
export interface DBSettings {
    dbType: DBType;
    prefix: string;
}
declare const _default: () => {
    db: DBSettings;
};
export default _default;
