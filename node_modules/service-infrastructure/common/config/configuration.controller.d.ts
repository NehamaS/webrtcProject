import { ConfigurationService } from "./configuration.service";
import { MculoggerService } from "../logs/mculogger.service";
export declare class ConfigurationController {
    private readonly configurationService;
    private readonly logger;
    constructor(configurationService: ConfigurationService, logger: MculoggerService);
    updateConfiguration(): Promise<any>;
    getAllJsonConfiguration(): Promise<any>;
    getKey(key: string): Promise<{
        key: string;
        value: string | number;
    }>;
    private getConfiguration;
}
