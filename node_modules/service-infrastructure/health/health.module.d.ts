import { BeforeApplicationShutdown } from '@nestjs/common';
import { MculoggerService } from '../common/logs/mculogger.service';
import { ConfigurationService } from '../common/config/configuration.service';
export declare const DEFAULT_EXIT_TIME: number;
export declare class HealthModule implements BeforeApplicationShutdown {
    private readonly logger;
    private readonly config;
    private shutDownTimer;
    constructor(logger: MculoggerService, config: ConfigurationService);
    beforeApplicationShutdown(signal?: string): Promise<void>;
    terminate(): void;
}
