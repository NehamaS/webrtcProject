import { HealthIndicator, HealthIndicatorResult } from '@nestjs/terminus';
import { IHealth, ProbType, ThriftServiceApiDef } from "../health.def";
import { MculoggerService } from "../../common/logs/mculogger.service";
import { ThriftPoolService } from "../../thrift/clientPool/thrift.pool.service";
export declare class ThriftService extends HealthIndicator implements IHealth {
    private readonly thriftPoolService;
    private logger;
    private probType;
    private thriftConfs;
    constructor(thriftPoolService: ThriftPoolService, logger: MculoggerService);
    check(): Promise<HealthIndicatorResult>;
    setProbType(probType?: ProbType): void;
    isHealthy(key: string): Promise<HealthIndicatorResult>;
    setThriftConf(thriftServiceApis: Array<ThriftServiceApiDef>): void;
}
