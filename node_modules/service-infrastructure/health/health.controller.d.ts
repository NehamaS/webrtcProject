import { HealthCheckResult } from '@nestjs/terminus';
import { ReadyService } from "./ready.service";
import { HealthService } from "./health.service";
import { MculoggerService } from "../common/logs/mculogger.service";
export declare class HealthController {
    private readonly readyService;
    private readonly healthyService;
    private readonly logger;
    constructor(readyService: ReadyService, healthyService: HealthService, logger: MculoggerService);
    check(isFromCache?: string): Promise<HealthCheckResult>;
    ready(isFromCache?: string): Promise<HealthCheckResult>;
}
