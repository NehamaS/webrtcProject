import { ServiceType } from "mcu-discovery/Discovery";
import { MculoggerService } from "../../common/logs/mculogger.service";
import { ConfigurationService } from "../../common/config/configuration.service";
import { IHealth, ProbType } from "../health.def";
import { HealthIndicator, HealthIndicatorResult } from '@nestjs/terminus';
export declare class DiscoveryService extends HealthIndicator implements IHealth {
    private configurationService;
    private logger;
    private ERROR;
    private discoveryConfs;
    private discovery;
    private probType;
    constructor(configurationService: ConfigurationService, logger: MculoggerService);
    check(): Promise<HealthIndicatorResult>;
    setProbType(probType?: ProbType): void;
    isHealthy(key: string): Promise<HealthIndicatorResult>;
    setDiscoveyConf(servicesName: Array<ServiceType>): Promise<void>;
    private getServiceAddress;
    private getServiceConf;
    private buildHealthDiscoveryList;
    private getDiscoveryObj;
}
