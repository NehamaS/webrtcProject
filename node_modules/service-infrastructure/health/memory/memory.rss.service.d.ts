import { HealthIndicatorResult, MemoryHealthIndicator } from '@nestjs/terminus';
import { ConfigurationService } from "../../common/config/configuration.service";
import { IHealth, ProbType } from "../health.def";
export declare class MemoryRssService extends MemoryHealthIndicator implements IHealth {
    private readonly configService;
    private probType;
    constructor(configService: ConfigurationService);
    check(): Promise<HealthIndicatorResult>;
    isHealthy(key: string): Promise<HealthIndicatorResult>;
    setProbType(probType?: ProbType): void;
}
