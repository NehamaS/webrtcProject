import { HealthIndicatorResult } from "@nestjs/terminus";
import { DiscoveryConf, ServiceType } from "mcu-discovery/Discovery";
export interface IHealth {
    check(): Promise<HealthIndicatorResult>;
    setProbType(probType?: ProbType): void;
}
export interface DiscoverableResponse {
}
export interface ThriftConnectionObj {
    serviceType: ServiceType;
    thriftServiceApi: any;
    discoveryConf?: DiscoveryConf;
}
export interface ThriftServiceApiDef {
    serviceType: ServiceType;
    thriftServiceApi: any;
}
export interface ServiceConf {
    name: string;
    domainSuffix: string;
    ttl?: number;
}
export declare enum ProbType {
    READY = 0,
    HEALTH = 1,
    LVN = 2
}
