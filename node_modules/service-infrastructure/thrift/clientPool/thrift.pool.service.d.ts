import { MculoggerService } from "../../common/logs/mculogger.service";
import { ServiceType } from "mcu-discovery/Discovery";
import { ThriftClient } from 'mcu-discovery/ThriftClient';
import { ConfigurationService } from "../../common/config/configuration.service";
export declare class ThriftPoolService {
    private readonly logger;
    private readonly configurationService;
    private pool;
    private fix_addr_pool;
    constructor(logger: MculoggerService, configurationService: ConfigurationService);
    getFixAddrConnection(ip: string, port: number, serverApi: any): Promise<ThriftClient>;
    getConnection(serviceType: ServiceType, thriftServiceApi: any): Promise<ThriftClient>;
    private buildServiceConf;
    private getConnectionPool;
    private buildConnectionPool;
    private getThriftConnectionObject;
}
