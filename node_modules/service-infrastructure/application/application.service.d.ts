import { OnApplicationBootstrap, OnApplicationShutdown } from '@nestjs/common';
import { MculoggerService } from "../common/logs/mculogger.service";
import { ConfigurationService } from '../common/config/configuration.service';
import { CpuService } from "../health/cpu/cpu.service";
export declare class ApplicationService implements OnApplicationBootstrap, OnApplicationShutdown {
    private readonly logger;
    private readonly configurationService;
    private readonly cpuService;
    private timeout;
    constructor(logger: MculoggerService, configurationService: ConfigurationService, cpuService: CpuService);
    monitorService(): void;
    private initProcess;
    onApplicationBootstrap(): any;
    onApplicationShutdown(signal?: string): any;
}
