import { OnApplicationBootstrap, OnModuleInit } from '@nestjs/common';
import { Persist } from 'mcu-persitence-service/persistance/persist';
import { ConfigurationService } from '../common/config/configuration.service';
import { MculoggerService } from '../common/logs/mculogger.service';
export declare class DbService implements OnModuleInit, OnApplicationBootstrap {
    private readonly logger;
    private configurationService;
    protected db: Persist;
    constructor(logger: MculoggerService, configurationService: ConfigurationService);
    private createConnection;
    onApplicationBootstrap(): void;
    onModuleInit(): Promise<void>;
    setRecord: <T>(key: string, value: T, ttl?: number) => Promise<boolean>;
    remove: (key: string) => Promise<boolean>;
    get: <T>(key: string) => Promise<T>;
    getItemsByPrefix: <T>(key: string) => Promise<T[]>;
    getNumOfRecords: (key: string) => Promise<number>;
    hasRecord: (key: string) => Promise<boolean>;
    getDB(): Persist;
}
