import { OnApplicationBootstrap } from '@nestjs/common';
import { MculoggerService } from '../common/logs/mculogger.service';
import { DbService } from "../db/db.service";
import { Gauge } from 'prom-client';
import { EtcdService } from '../health/etcd/etcd.service';
export declare class MetricsService implements OnApplicationBootstrap {
    private readonly logger;
    private readonly dbSrv;
    private etcdService;
    private metricsRegistry;
    etcd_readwrite: Gauge<any>;
    constructor(logger: MculoggerService, dbSrv: DbService, etcdService: EtcdService);
    onApplicationBootstrap(): void;
    etcdMetrics(): Promise<string>;
    collect(): Promise<String>;
}
