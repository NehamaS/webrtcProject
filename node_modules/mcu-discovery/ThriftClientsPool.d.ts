import { ThriftClient } from './ThriftClient.js';
import { ConnConfig } from './ThriftClient.js';
import { ServerIp } from "./ClientDiscovery";
export declare class ThriftClientsPool {
    private serverApi;
    private connConfig;
    private logger;
    clientsMap: Map<string, ThriftClient>;
    constructor(serverApi: any, connConfig: ConnConfig, logger: any);
    private addClient;
    getClient(serverAddress: ServerIp): Promise<ThriftClient>;
}
