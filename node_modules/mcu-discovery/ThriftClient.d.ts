export interface ConnConfig {
    maxAttempts: number;
    retryMaxDelay: number;
    connectTimeout: number;
    debug: boolean;
}
export declare class ThriftClient {
    private clientAddress;
    private clientPort;
    private serverApi;
    private connConfig;
    private logger;
    connection: any;
    client: any;
    error: string;
    constructor(clientAddress: string, clientPort: number, serverApi: any, connConfig: ConnConfig, logger: any);
    connect(): Promise<unknown>;
    isConnected(): any;
    getClient(): any;
    getIp(): string;
    getPort(): number;
}
